# 安全政策

当前维护范围为最新预发布版本，尚无固定响应时限或长期支持承诺。

请不要在公开 Issue 中发布可利用的漏洞细节、令牌、私人存档或账户信息。如果仓库 Security → Advisories 显示 “Report a vulnerability”，优先使用该私密入口；否则先开一个只写“请求私密安全报告渠道”的 Issue，等待维护者提供联系方法，再分享敏感细节。普通崩溃和兼容问题使用 Bug 模板即可。

依赖通过 HTTPS 获取并按固定 SHA256 校验。离线启动不下载更新。第三方模组是可执行 Lua，应仅安装可信内容。本版本未提供自动更新器、代码签名、沙箱逃逸防护承诺或服务器安全审计。不要在公开网络上运行测试入口。
