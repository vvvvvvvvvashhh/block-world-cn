# 贡献指南 / Contributing

欢迎修复、中文翻译、性能分析、测试与文档贡献。先搜索已有 Issue；较大玩法或架构变更先描述目标与验收方式，再提交 PR。

1. Fork 仓库，在独立分支修改。src 和 assets 是定制源文件，overrides.json 决定部署位置。
2. 使用 dependencies.lock.json 中固定的包；更换上游版本时同时更新来源、SHA256、许可和变更记录。
3. 提交前运行 `python scripts/check.py`；Windows 下运行 `scripts/launch.ps1 -Check`。涉及游戏逻辑时提供实际操作、种子、预期/实际结果和适量脱敏截图。
4. PR 说明问题、最终行为、验证方法和已知限制。小改动保持聚焦，不要混入不相关格式化。
5. 不提交个人存档、日志、缓存、令牌或专有游戏素材。报错日志先删除账户、机器路径及其他私人信息。

本仓库代码贡献按 GPL-3.0-or-later 提供；上游文件保持原许可与作者信息，素材须附兼容许可及来源。不要把整个项目重新标为 MIT，也不要移除原作者署名。几何 UI 生成器与其产物为 CC0。无需 CLA。

测试结果必须区分真实键鼠操作、引擎回调、提供测试材料和仅检查模块注册。不要把未覆盖的内容写成已通过。AI 辅助贡献同样需要可读代码、许可检查和可复现验证。
